import { withPageTransition } from '@renderer/components/AnimatedOutlet'
import styles from './styles.module.scss'

const WebContacts = () => {
  return <div>WebContacts</div>
}

export default withPageTransition(WebContacts)
