#!/bin/bash
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/../common.sh"

# todo: This script creates a new page with the necessary files and boilerplate code. Implement this after creating the updates page.